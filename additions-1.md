# ADDITIONS 1 — update to Hackaton.md (16 Aug, ~12:00)

Only one change to the team file. Everything else in Hackaton.md stands.

WHERE: §2 "Adjustment terms (shared)" — add this row to the list:

  evidence      = LLM output, TWO-SPEED: quantified+corroborated → routed to a
                  deterministic row at FULL weight (guidance→gap, buyback→row);
                  qualitative/unquantified → capped ±2% (money) / ±0.5pp (rate).
                  LLM NEVER computes a number — it only returns typed specs.

WHY IT MATTERS (one line each):
- It formalises the anti-hallucination / anti-overreaction guard: a hard number
  gets real weight; a vague "CEO sounded cautious" gets almost none.
- It confirms the deterministic-core rule: the LLM reads and returns typed
  specs; code does all arithmetic. No number is ever produced by the model.

NO OTHER CHANGES. The four families, the two arithmetic modes (levels multiply,
rates add), the derived-profit rule, the work split, build order, traps and
pitch are all unchanged.